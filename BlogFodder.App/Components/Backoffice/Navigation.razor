@foreach (var settingsLink in PluginSettings)
{
    <div class="nav-item px-3">
        <NavLink class="nav-link" href="@settingsLink.Route">
            <span class="oi oi-plus" aria-hidden="true"></span> @settingsLink.Text
        </NavLink>
    </div>
}