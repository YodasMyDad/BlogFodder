@model List<string>

@foreach (var cssFile in Model)
{
    <link rel="stylesheet" href="@cssFile" @Html.Raw(cssFile.StartsWith("http") ? "crossorigin=\"anonymous\"" : "") asp-append-version="true"/>
}